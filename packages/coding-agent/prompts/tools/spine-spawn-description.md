Fission the current work into two to four concurrent peer branches created from the current full history. Each branch must own a semantically independent direction that resolves a concrete uncertainty or produces an independently verifiable outcome, with an explicit scope, evidence boundary, and completion predicate. Give every branch a concise summary unique within this call; the runtime uses it as the branch's public identity. Each branch must be able to finish with a bounded fallback without another branch's result and must return one terminal memory. The parent is suspended while branches run; no supervisory model remains active. Join waits for every branch and imports their terminal results as closed task nodes in input order before resuming the parent. Call spine_spawn at most once in one model response and place every concurrent branch in that call. Use it only when at least two substantial, self-contained branches gain materially from concurrency; do not create paraphrased branches over the same tightly coupled question unless they are independent replication or falsification. If branches need coordination, the parent must first create a task-local shared blackboard and repeat the same `Shared blackboard: <path>` line in every prompt. Shared-workspace and external effects are non-transactional, so production-file writes require disjoint ownership or one explicitly named integration owner. Child sessions cannot call spine_spawn.
