Use spine_spawn at most once in a model response and only for two to four substantial, self-contained, independently completable branches. Give each branch a distinct scope, evidence boundary, completion predicate, and unique summary. Put every ready branch in the same ordered tasks array; child sessions cannot call spine_spawn. For production-file changes, assign disjoint ownership or one integration owner. Resume only after every branch settles, then evaluate and synthesize the imported memories in task order.
